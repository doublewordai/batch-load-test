apiVersion: v1
kind: Service
metadata:
  name: {{ include "mockai.fullname" . }}
  labels:
    {{- include "mockai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "mockai.selectorLabels" . | nindent 4 }}
